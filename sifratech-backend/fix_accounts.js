require('dotenv').config(); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_KEY); async function run() { const { data: tickets } = await supabase.from('tickets').select('email_address, customer_name'); const uniqueCustomers = {}; tickets.forEach(t => { if(t.email_address) uniqueCustomers[t.email_address] = t.customer_name; }); for (const email of Object.keys(uniqueCustomers)) { try { await supabase.auth.admin.createUser({ email, password: 'Welcome@2026', email_confirm: true, user_metadata: { full_name: uniqueCustomers[email] } }); console.log('Created auth for', email); } catch(e) {} try { await supabase.from('customer_accounts').insert([{ email, plain_password: 'Welcome@2026' }]); console.log('Inserted into customer_accounts', email); } catch(e) {} } console.log('Done'); } run();
